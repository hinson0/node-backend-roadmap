你遇到的核心问题不是“AI 写了代码”，而是你把“理解、判断、验证”也一起外包了。

想重新 hold 住项目，不需要掌握每一行细节。真正需要掌握的是：

- 系统由哪些模块组成
- 一次请求如何流转
- 每个模块的输入、输出和职责
- 修改一个地方会影响哪里
- 出错时如何用证据缩小范围

从现在开始，可以把 AI 的使用方式改成下面这样。

### 1. 不再直接要完整答案

学习阶段，让 AI 按这个顺序协作：

1. 先解释问题和修改思路
2. 列出会改哪些文件、为什么
3. 一次只给一个小步骤
4. 你亲手写代码
5. AI 只审查、提示，不直接替你改
6. 最后由你解释代码为何有效

如果你无法用自己的话解释这次修改，它就还不算完成。

### 2. 每次写代码前，先回答四个问题

- 这段代码解决什么问题？
- 输入是什么？
- 输出是什么？
- 失败时会发生什么？

例如一个 `sleep(ms)` 函数，你至少要能说清：

> 输入是毫秒数；输出是 `Promise<void>`；计时结束后 Promise 成功；它本身没有业务返回值。

你不需要记住所有语法，但要理解行为和边界。

### 3. 强制把改动切小

不要让 AI 一次生成十几个文件。每次只完成一个可验证的小目标，例如：

- 先定义类型
- 再实现纯函数
- 再接数据库
- 再接路由
- 最后补异常处理

改动越小，问题越容易定位，你也越容易建立因果关系。

### 4. 专门训练调试，而不只是写功能

遇到错误时，先不要问 AI“怎么修”。先自己写下：

1. 预期发生什么？
2. 实际发生什么？
3. 最小复现方式是什么？
4. 最后一个正常的位置在哪里？
5. 当前有哪些证据？
6. 我猜测的原因是什么？

然后让 AI 评估你的推断。这样 AI 是你的教练，而不是代替你思考的人。

调试的固定顺序可以是：

```text
稳定复现 → 缩小范围 → 提出假设 → 收集证据 → 修复 → 回归验证
```

不要一看到报错就随机改代码。

### 5. 给项目维护一张“心智地图”

在项目中维护一个简单的 `ARCHITECTURE.md`，记录：

- 目录分别负责什么
- 程序入口在哪里
- 一次请求经过哪些模块
- 核心数据结构
- 外部依赖
- 常用调试命令
- 当前还没理解的地方

每完成一个功能，就更新这张地图。长期来看，这比记住每一行代码重要得多。

### 6. 区分两种工作模式

学习模式：

- 你写代码
- AI 给提示和审查
- 卡住时先给线索，再逐步增加帮助

交付模式：

- AI 可以实现
- 但必须说明改动范围、关键决策、验证结果
- 你至少亲自阅读入口、核心逻辑和错误处理

不能一直使用交付模式，否则理解债务会越积越多。

### 7. 从今天开始执行一个简单规则

以后你可以直接对我说：

> 进入学习模式。不要替我修改代码。一次只给一个步骤，先让我回答和动手；我卡住时先给提示，不要直接给答案。

针对你现在的 TypeScript 学习，我还会先让你预测“这段代码能否通过类型检查、返回什么类型、为什么”，再让你运行验证。

目标不是脱离 AI，而是把 AI 从“代写者”变成“陪练和审查者”。你最终需要拥有的能力不是默写代码，而是能解释、能验证、能定位、敢修改。
